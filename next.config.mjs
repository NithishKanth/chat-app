/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.clerk.com','images.clerk.dev'], // Add other domains if needed
    },
};

export default nextConfig;
